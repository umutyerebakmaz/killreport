query Top90DaysPilots($filter: Top90DaysPilotsFilter) {
  top90DaysPilots(filter: $filter) {
    rank
    killCount
    character {
      id
      name
      securityStatus
      corporation {
        id
        name
      }
      alliance {
        id
        name
      }
    }
  }
}
