query SearchAlliances($search: String!, $limit: Int = 40) {
  alliances(filter: { search: $search, limit: $limit }) {
    items {
      id
      name
      ticker
      memberCount
      corporationCount
    }
  }
}
