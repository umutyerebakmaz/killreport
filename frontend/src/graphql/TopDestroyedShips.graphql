query TopDestroyedShips($filter: TopFilter) {
  topDestroyedShips(filter: $filter) {
    rank
    killCount
    shipType {
      id
      name
      dogmaAttributes {
        attribute_id
        value
      }
    }
  }
}
