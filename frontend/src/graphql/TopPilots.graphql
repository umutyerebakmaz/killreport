query TopPilots($filter: TopPilotsFilter) {
  topPilots(filter: $filter) {
    rank
    killCount
    character {
      id
      name
      securityStatus
      corporation {
        id
        name
        ticker
      }
      alliance {
        id
        name
        ticker
      }
    }
  }
}
