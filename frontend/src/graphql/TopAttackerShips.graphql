query TopAttackerShips($filter: TopFilter) {
  topAttackerShips(filter: $filter) {
    rank
    killCount
    shipType {
      id
      name
      dogmaAttributes {
        attribute_id
        value
      }
    }
  }
}
