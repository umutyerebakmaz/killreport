query TopWeeklyPilots($filter: TopWeeklyPilotsFilter) {
  topWeeklyPilots(filter: $filter) {
    rank
    killCount
    character {
      id
      name
      securityStatus
      corporation {
        id
        name
      }
      alliance {
        id
        name
      }
    }
  }
}
