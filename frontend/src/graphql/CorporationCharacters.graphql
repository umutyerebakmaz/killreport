query CorporationCharacters($filter: CharacterFilter) {
  characters(filter: $filter) {
    items {
      id
      name
      securityStatus
    }
    pageInfo {
      currentPage
      totalPages
      totalCount
      hasNextPage
      hasPreviousPage
    }
  }
}
