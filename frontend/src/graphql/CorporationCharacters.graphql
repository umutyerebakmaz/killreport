query CorporationCharacters($filter: CharacterFilter) {
  characters(filter: $filter) {
    items {
      id
      name
      security_status
    }
    pageInfo {
      currentPage
      totalPages
      totalCount
      hasNextPage
      hasPreviousPage
    }
  }
}
