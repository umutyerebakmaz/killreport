query SearchSolarSystems($search: String!, $limit: Int = 40) {
  solarSystems(filter: { search: $search, limit: $limit }) {
    items {
      id
      name
      securityStatus
      security_class
      constellation {
        id
        name
        region {
          id
          name
        }
      }
    }
  }
}
