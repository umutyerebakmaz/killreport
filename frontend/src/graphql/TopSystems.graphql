query TopSystems($filter: TopFilter) {
  topSystems(filter: $filter) {
    rank
    killCount
    solarSystem {
      id
      name
      securityStatus
      constellation {
        region {
          name
        }
      }
    }
  }
}
