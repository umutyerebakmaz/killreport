query TopLast7DaysShips($filter: TopLast7DaysShipsFilter) {
  topLast7DaysShips(filter: $filter) {
    rank
    killCount
    shipType {
      id
      name
      dogmaAttributes {
        attribute_id
        value
      }
    }
  }
}
