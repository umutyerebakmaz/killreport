query SearchCharacters($search: String!, $limit: Int = 40) {
  characters(filter: { search: $search, limit: $limit }) {
    items {
      id
      name
      corporation {
        id
        name
        ticker
      }
      alliance {
        id
        name
        ticker
      }
    }
  }
}
