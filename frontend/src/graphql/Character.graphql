query Character($id: Int!) {
  character(id: $id) {
    id
    name
    birthday
    security_status
    description
    title
    updatedAt
    corporation {
      id
      name
      ticker
    }
    alliance {
      id
      name
      ticker
    }
  }
}
