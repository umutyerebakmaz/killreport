query Character($id: Int!) {
  character(id: $id) {
    id
    name
    birthday
    security_status
    gender
    description
    title
    updatedAt
    corporation {
      id
      name
      ticker
      member_count
    }
    alliance {
      id
      name
      ticker
      memberCount
    }
    race {
      id
      name
    }
    bloodline {
      id
      name
    }
  }
}
