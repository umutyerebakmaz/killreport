query SearchCorporations($search: String!, $limit: Int = 40) {
  corporations(filter: { search: $search, limit: $limit }) {
    items {
      id
      name
      ticker
      member_count
      alliance {
        id
        name
        ticker
      }
    }
  }
}
