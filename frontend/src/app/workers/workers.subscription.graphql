subscription WorkerStatusSubscription {
  workerStatusUpdates {
    timestamp
    healthy
    queues {
      name
      messageCount
      consumerCount
      active
      workerRunning
      workerPid
      workerName
    }
    standaloneWorkers {
      name
      running
      pid
      description
    }
  }
}
