subscription WorkerStatusSubscription {
  workerStatusUpdates {
    timestamp
    healthy
    queues {
      name
      messageCount
      consumerCount
      active
    }
    standaloneWorkers {
      name
      running
      pid
      description
    }
  }
}
