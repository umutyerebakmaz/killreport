{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "permissions": {
    "deny": [
      "Bash(npx prisma migrate dev:*)",
      "Bash(npx prisma migrate reset:*)",
      "Bash(yarn prisma:migrate)",
      "Bash(yarn workspace backend prisma:migrate)"
    ]
  },
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "cmd=$(node -e 'let s=\"\";process.stdin.on(\"data\",d=>s+=d).on(\"end\",()=>{try{process.stdout.write(JSON.parse(s).tool_input?.command??\"\")}catch{}})'); if printf '%s' \"$cmd\" | grep -qE '(^|[|;&][[:space:]]*)((npx|bunx)[[:space:]]+)?prisma[[:space:]]+migrate[[:space:]]+(dev|reset)([[:space:]]|$)|(^|[|;&][[:space:]]*)(yarn|npm[[:space:]]+run|pnpm[[:space:]]+run|bun[[:space:]]+run)[[:space:]]+(workspace[[:space:]]+[A-Za-z0-9_@/-]+[[:space:]]+)?prisma:migrate([[:space:]]|$)|prisma[[:space:]]+db[[:space:]]+push[^|;&]*(--force-reset|--accept-data-loss)'; then printf '%s' '{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"permissionDecision\":\"deny\",\"permissionDecisionReason\":\"BLOCKED by .claude/settings.json: this command can drop killmail_filters, character_kill_stats, corporation_kill_stats, alliance_kill_stats and refresh_log. See CLAUDE.md > Database migrations: build the migration with prisma migrate diff and apply it with prisma migrate deploy.\"}}'; fi",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
