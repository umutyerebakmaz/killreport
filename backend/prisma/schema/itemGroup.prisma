model ItemGroup {
  id          Int      @id
  name        String
  category_id Int
  published   Boolean  @default(true)
  created_at  DateTime @default(now())
  updated_at  DateTime @updatedAt

  @@index([category_id])
  @@map("item_groups")
}
